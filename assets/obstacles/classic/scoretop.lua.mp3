function init()
	b = mgCreateBody()
	
	mgMaterial("glass")
	mgColor(0.5, 1, 0.5)
	
	mgShapeScale(0.2, 1.0, 0.2)
	mgCreateMesh(b, "pyramid")

	done = false
end

function tick()
	if mgBodyIsBroken(b) and not done then
		mgScore(500, 0, 0.5, 0)
		done = true
	end
end


